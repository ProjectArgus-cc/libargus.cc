name: CI
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  actions: read
jobs:
  validation:
    uses: ./.github/workflows/validation.yml
    with:
      source: ${{ github.sha }}
      release: false
